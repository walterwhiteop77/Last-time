# Required — set ALL of these in Render > Environment
BOT_TOKEN=123456:ABCdef_from_BotFather
API_ID=12345678
API_HASH=your_api_hash_from_my.telegram.org
MONGODB_URI=mongodb+srv://<user>:<pass>@cluster.mongodb.net/

# Optional — seeds the FIRST userbot account.
# Leave empty and add every account with /login in the admin bot instead.
# All accounts are stored in MongoDB, so they survive restarts.
SESSION_STRING=

# PORT is injected by Render automatically — do not set manually
